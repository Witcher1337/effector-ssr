import { BaseTemplate } from '../../shared/ui/templates';
import { PrimaryHeader } from '../../widgets';
import styles from './styles.module.scss';

export const Home = () => (
  <p className={styles.text}>
    This is a home page
  </p>
)